# SeeWeeS Dashboard Prototype

Interactive Next.js prototype for the UCLA MSBA AI Agents Project Challenge dashboard.

## Local Development

```bash
cd web
npm install
npm run dev
```

Open `http://localhost:3000`.

## Vercel

Deploy this folder as the Vercel project root. The first version uses mocked scenario data shaped to match future LangGraph agent output.
